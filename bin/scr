#!/usr/bin/env python3
#
# scr - a screen helper script to manage screen sessions with a default 
#       set of names that are always offered active or not.
# author: Ed Pollard
#         I mostly wrote this to try out a few ideas
#
import click
#from scr.screen import Screens
from scr.disp import ScrDisplay
from scr.config import Config


@click.command()
@click.option('-n','--nocolor',is_flag=True, default=False)
@click.option('-d','--default_sessions', type=str, default="",
              help="Comma separated list of session strings.")
@click.option('-c','--curses', is_flag=True,
              help='Set interface to a curses style.')
@click.option('-t','--text', is_flag=True,
              help='Set interface to a text style. [Default]')

@click.pass_context
def get_args(ctx, **kwargs):
    config = Config(kwargs)
    sd = ScrDisplay("Screen Session Menu",config=config)
    sd.menu()


if __name__ == "__main__":
    get_args()


